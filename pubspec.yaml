name: bmi_calculator_th
description: BMI Calculator in Thai application.

version: 1.0.4+5

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  admob_flutter: ^1.0.1
  auto_size_text: ^2.1.0
  font_awesome_flutter: ^8.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - images/